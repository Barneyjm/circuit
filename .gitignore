.venv/
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/
.ruff_cache/
results/*.log

# secrets
.env
.env.*
.runpod_pod.json

# rendered vision-grid images are regenerated from the seed
data/vision/grid/train/
data/vision/grid/eval/

# built by scripts/build_unseen_eval.py from the seed; non-commercial text and COCO images
data/unseen_eval.jsonl
data/vision/unseen/

# trained adapters live on the Hub, not in git
runs/
# datasets with teacher-model (Jev/Gemini) targets stay local
data/train.jsonl
data/eval.jsonl
data/train_real_plus_syn.jsonl
data/human_subset.jsonl
data/human_sample.json
wandb/
.DS_Store
# synthesized audio-grid clips are regenerated from the seed (macOS say)
data/audio/grid/train/
data/audio/grid/eval/
# real-recording sources are downloaded by the generator docs (LibriSpeech dev-clean, FSDD)
data/audio/sources/
# downloaded photo sources (Open Images, per-image attribution in index.json)
data/vision/sources/

# output of scripts/reproduce_unseen.sh
results/repro/
data/hard_tier_*.jsonl
data/hard_mix_train.jsonl
